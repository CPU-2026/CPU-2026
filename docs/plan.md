# CPU-2026：实现计划

## 目标与现状

目标是支持 RV32IM、乱序执行与按序提交的多发射核心：多宽取指/译码/重命名/分配，独立整数指令可多发射，乘除、分支与访存单元保留。参数覆盖发射宽度、ROB、PRF、RS 及缓存容量/相联度。课程接口要求 `student_top`、高有效 `reset`、32 位 AXI4-Lite；程序以向 `0x80000000` 全字写（`WSTRB=4'hf`）并收到 **B 通道响应**结束。

| 验收项 | 课程最高档 / 额外频率分                 | 工程余量目标 |
| ------ | --------------------------------------- | ------------ |
| 正确性 | 19 项通过                               | 始终回归     |
| 性能   | 6 项 `perf_*` 的几何平均 IPC ≥1.0985    | ≥1.12        |
| 总面积 | ≤36,000 μm²（标准单元 + 全部 FakeRAM）  | ≤34,000 μm²  |
| 频率   | 最高档 ≥300 MHz；≥500 MHz 可获额外 4 分 | ≥525 MHz     |

- **已有核心**：源码由 `verilog/filelist.f` 列出，单宽 RV32IM 乱序核已有 CLA 整数/乘除单元、中心化的48容量物理寄存器堆PRF 、RAT、16容量的ROB、FQ/IQ 各 4 槽、有界 RS、LQ/SQ 各 8 槽、访存单元（DMEM+IMEM+D$+I$）、四条CDB总线、顺序提交和分支恢复。
- **预测与缓存**：Tournament 三张 256×2 bit 表、推测 GHR8、BTB64（56 bit/项）、RAS8、SARAS16、condSeen512、32 项取指检查点及 16 项 RAS 日志；8 KiB 直接映射 I$、64 KiB 四路 D$，支持脏行写回和原带外 HALT 排空。
- **接口缺口**：`cpu_top` 是无缓存原生接口，`cpu_cached_top` 使用 128 位缓存行；两者均为低有效复位和带外 HALT，尚无 `student_top`、AXI4-Lite、MMIO 退出和 `sram_fakeram`。
- **吞吐瓶颈**：核心每周期仅一次 `issue_fire`、一次 ROB 提交，仅增加执行单元或 RS 容量无法突破长期 IPC ≤1；前端单在途取指、I$ 命中 TAG_READ 与阻塞式 D$ 也限制双宽吞吐。

## 12 周计划

| 周次       | 重点与交付                                                                                                                                                |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| W1         | 根据模拟器实现与架构文档理解目前核心的微架构，理解其余项目文档；了解sv的基本语法，理解当前实现模块的细节                                                  |
| W2         | 实现单发射的`student_top`，目标完成basic+correctness测试，建立首份单发射性能测试基线（含 SRAM 面积）                                                      |
| W3         | 达成单发射的性能目标（阶段1），完成单发射下的参数敏感度分析与架构探索总结                                                                                 |
| W4         | 实现取指与译码的可参数化superscalar                                                                                                                       |
| W5         | 实现重命名与寄存器分配的可参数化superscalar                                                                                                               |
| W6         | 实现发射队列与ROB退休的可参数化superscalar                                                                                                                |
| W7（中期） | 实现多发射的`student_top`，目标完成basic+correctness测试，建立首份多发射性能测试基线（含 SRAM 面积）                                                      |
| W8         | 达成多发射的性能目标（阶段2），完成多发射下的参数敏感度分析与架构探索总结                                                                                 |
| W9         | 根据主要面积模块与ipc优化点进行优化，尝试完成阶段3的目标                                                                                                  |
| W10-W11    | 结合 `timing.rpt` 和面积报告修 RAT/PRF/RS/ROB/BPU/Cache 关键路径，确定 SRAM 布局及缓存几何；目标总面积 ≤36,000 μm²、估计频率 >500 MHz，守住正确性和 IPC。 |
| W12        | 完成最终交付cpu的参数敏感度分析与架构探索总结，提交仓库与文档                                                                                             |